# AscendoreCRM Production Environment Configuration
# This file should be configured in AWS Secrets Manager or ECS Task Definition

# Server Configuration
NODE_ENV=production
PORT=3001
API_VERSION=v1

# Database Configuration (Shared with Overlord)
# These values are populated from Overlord's stack outputs
DB_HOST=${DB_HOST}
DB_PORT=5432
DB_NAME=overlord
DB_USER=${DB_USER}
DB_PASSWORD=${DB_PASSWORD}

# Redis Configuration (Shared with Overlord)
REDIS_HOST=${REDIS_HOST}
REDIS_PORT=6379

# JWT Configuration (Shared with Overlord)
JWT_SECRET=${JWT_SECRET}
JWT_EXPIRES_IN=7d

# CORS Configuration
CORS_ORIGIN=https://theoverlord.ai,https://www.theoverlord.ai,https://crm-api.theoverlord.ai

# AI Services (Anthropic Claude)
ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
ANTHROPIC_MODEL=claude-3-5-sonnet-20241022

# AWS Configuration
AWS_REGION=us-east-1

# Logging
LOG_LEVEL=info
CLOUDWATCH_LOG_GROUP=/aws/ascendore-crm/prod/api

# Feature Flags
ENABLE_AI_FEATURES=true
